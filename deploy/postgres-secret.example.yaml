apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: mem-bank
type: Opaque
data:
  # Base64 encoded values - replace with your actual values
  # echo -n "your_actual_database_name" | base64
  POSTGRES_DB: bWVtX2Jhbms=  # mem_bank
  # echo -n "your_actual_username" | base64  
  POSTGRES_USER: bWVtX2JhbmtfdXNlcg==  # mem_bank_user
  # echo -n "your_actual_password" | base64
  POSTGRES_PASSWORD: Y2hhbmdlX21lX2luX3Byb2R1Y3Rpb24=  # change_me_in_production
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: mem-bank
data:
  POSTGRES_DB: "mem_bank"
  POSTGRES_USER: "mem_bank_user"